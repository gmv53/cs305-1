#include <stdio.h>

void main()
	{
		// initialize variables
		char letter;
		int number;
		
		//This note is actually useless

		// display message prompting user for letter
			// function: printf

		// get and save user letter
			// function: scanf

		// display message prompting user for number
			// function: printf

		// get and save user number
			// function: scanf
			
		//This note is useless as well :(

		// display letter the given number of times
		while( number > 0 )
			{
				// print the letter
				printf( "%c", letter );

				// decrement number
				number--;
			}
	}
